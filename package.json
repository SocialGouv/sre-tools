{
  "name": "@socialgouv/sre-tools",
  "private": true,
  "version": "1.0.0",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "lint": "lerna run --parallel --stream lint",
    "test": "lerna run --parallel --stream test"
  }
}
